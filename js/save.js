function saveToFile(data){
    
    var dataToSave = 'var dataToRead={';
    
    for(var i=0; i<data.length;i++)
        console.log(data[i].settings.type);
       // dataToSave += data[i];
    
    
    
}